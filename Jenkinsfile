/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    triggers {
        githubPush()
    }
    stages {
        stage('build') {
            steps {
                echo "testingg"
            }
        }
    }
}
